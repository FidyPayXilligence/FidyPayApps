# Tomcat server
server.tomcat.threads.max=400
server.port=8007

logging.file.path=/usr/local/logs/FP_DashBoard

# Mysql DB connection settings - AWS_LIVE
spring.datasource.url=jdbc:mysql://fidypay-prod.cljgt4wfgxae.ap-south-1.rds.amazonaws.com:3306/FP_IN?sslmode=required
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=admin
spring.datasource.password=hl8WnjL0CKEy0i2v0o6P

#PROD
spring.main.allow-bean-definition-overriding=true
logging.level.tracer=TRACE

# HikariCP settings
spring.datasource.hikari.connection-timeout=660000
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.max-lifetime=660000
spring.datasource.hikari.maximum-pool-size=1000

# Connect timeout (time to establish the connection to the server) in milliseconds
spring.http.client.connect-timeout = 300000

# Read timeout (maximum time to wait for data from the server) in milliseconds
spring.http.client.read-timeout = 300000

# JPA settings
server.servlet.context-path=/dashboard
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

#AWS s3 credentials
amazonProperties.endpointUrl=https://fidylogoandqrimages.s3.ap-south-1.amazonaws.com
s3.bucket=fidylogoandqrimages

# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Max file size.
spring.servlet.multipart.max-file-size=15MB
# Max Request Size
spring.servlet.multipart.max-request-size=20MB